import React,{Component} from "react"class Todo extends Component{    constructor(){        super();    }    render(){        const style = {            fontStyle: "italic",            color: "#f00",            textDecoration: "line-through"        }        return(            <div style={{ backgroundColor:"lightblue", padding:25,display:"inline-block" }}>                <input                        type="checkbox"                        checked={this.props.data.completed}                        onChange={()=>this.props.myHandleChange(this.props.data.id)}                />                <p style={ this.props.data.completed ? style :null}>{this.props.data.text}</p>            </div>        )    }}export default Todo